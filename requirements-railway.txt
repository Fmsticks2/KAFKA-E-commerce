# Railway deployment requirements - without confluent-kafka
# Using embedded Kafka instead to avoid librdkafka compilation issues

flask==2.3.3
flask-cors==4.0.0
structlog==23.1.0
python-dotenv==1.0.0
prometheus-client==0.17.1
requests==2.31.0
jsonschema==4.19.0
retrying==1.3.4
psutil==5.9.5
coloredlogs==15.0.1
pyyaml==6.0.1

# Production dependencies
gunicorn==21.2.0
gevent==23.7.0

# Alternative Kafka client (if needed)
kafka-python==2.0.2